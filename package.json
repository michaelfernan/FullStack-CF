{
  "name": "fullstack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "start": "docker-compose -f ./docker-compose.yml up --build",
    "start-db": "docker-compose -f ./docker-compose.yml up -d db",
    "start-backend": "cd backend && npm start",
    "start-frontend": "cd frontend && npm start",
    "start-local": "npm run start-db && npm run start-backend && npm run start-frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "dependencies": {
    "vue-router": "^4.4.2"
  }
}
